package com.jnt.echarts.options.code;

public enum SeriesType {
    line, lines, bar, scatter, effectScatter, candlestick, pie, radar, graph, map, funnel, gauge, treemap, heatmap, boxplot, parallel, sankey
}
